import { GoogleGenerativeAI } from '@google/generative-ai'; async function test() { const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY || ''); try { const model = genAI.getGenerativeModel({ model: 'gemini-1.5-flash', tools: [{ googleSearchRetrieval: {} }] }); const res = await model.generateContent('Who won the 2024 Super Bowl?'); console.log('SUCCESS:', res.response.text()); } catch (e: any) { console.error('ERROR:', e.message); } } test();
